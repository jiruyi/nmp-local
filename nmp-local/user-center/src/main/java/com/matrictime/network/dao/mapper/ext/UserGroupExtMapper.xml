<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.matrictime.network.dao.mapper.ext.UserGroupExtMapper">
    <resultMap id="BaseResultMap" type="com.matrictime.network.dao.model.UserGroup">
        <result column="user_id" jdbcType="BIGINT" property="userId" />
        <result column="group_id" jdbcType="BIGINT" property="groupId" />
        <result column="is_exist" jdbcType="BIT" property="isExist" />
    </resultMap>

    <update id="updateByUserIdAndGroupId" parameterType="com.matrictime.network.api.request.UserGroupReq">
        update user_group
        <set>
            <if test="targetGroupId != null">
                group_id = #{targetGroupId,jdbcType=BIGINT},
            </if>
            <if test="isExist != null">
                is_exist = #{isExist,jdbcType=BIT},
            </if>
        </set>
        where user_id = #{userId,jdbcType=BIGINT} and
        group_id = #{groupId,jdbcType=BIGINT}
    </update>

    <select id="selectByCondition" resultType="com.matrictime.network.api.response.UserGroupResp">
        select * from user_group a,user b where a.user_id=b.user_id
        <trim prefix="(" suffix=")" suffixOverrides="and">
            <if test="groupId != null">
                and  group_id = #{targetGroupId,jdbcType=BIGINT}
            </if>
            <if test="userId != null">
                and user_id = #{userId,jdbcType=BIGINT}
            </if>
            <if test="isExist != null">
                and is_exist = #{isExist,jdbcType=BIT}
            </if>
        </trim>

    </select>


</mapper>