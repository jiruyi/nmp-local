server:
  port: 8006                                                # 密区端口默认值（8006）; 非密区端口修改为默认值（8007）   （需修改项，如果不设置为默认值，需要通知关联系统）

spring:
  application:
    name: UserCenter
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/user_center            # 数据库名称（可根据自定义情况配置）
    username: root                                          # 数据库用户名（可根据自定义情况配置）
    password: Jzsg@2021                                     # 数据库密码（可根据自定义情况配置）
    druid:
      min-idle: 5
      initial-size: 5
      max-total: 5
      max-wait-millis: 200

logging:
  file:
    path: ./logs
  level:
    com.jzsg: info

app:
  handleType: 1                                             # 密区部署值为 1 ;非密区部署值为 0 （需修改项）
  flowtype: 0                                               # 密区部署值为 0 ;非密区部署值为 1 （需修改项）
  name: userCenterProd
  id: UC999
  port: 9999
  timeout: 5000
  innerUrl: http://127.0.0.1:8008
  outerUrl: http://127.0.0.1:8009
